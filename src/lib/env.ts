export const environment = {
	providerEndpoint: import.meta.env.VITE_PROVIDER_ENDPOINT as string,
	apiEndpoint: import.meta.env.VITE_API_ENDPOINT as string,
	networkId: import.meta.env.VITE_NETWORK_ID as string,
	midiAddress: import.meta.env.VITE_MIDI_ADDRESS as string,
	midiDeployBlock: import.meta.env.VITE_MIDI_DEPLOY_BLOCK as string,
	marketAddress: import.meta.env.VITE_MARKET_ADDRESS as string
};
